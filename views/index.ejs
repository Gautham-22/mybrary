<h1>Recently Added Books</h1>

<% books.forEach((book) => { %>
    <div>
        <p><%= book.title %></p>
        <img src="/uploads/coverfiles/<%= book.coverImageName %>" alt="book" width="220" height="250">  
    </div>
<% }); %>